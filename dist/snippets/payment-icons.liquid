{%- comment -%}
  Inputs:
    nothing
{%- endcomment -%}

{%- if shop.enabled_payment_types.size > 0 -%}
  <div class="flex flex-wrap gap-2">
    {%- for type in shop.enabled_payment_types -%}
      <div>
        {{ type | payment_type_svg_tag }}
      </div>
    {%- endfor -%}
  </div>
{%- else -%}
  
  <img class="mt-2 mx-auto desktop:m-0" src="{{ 'icon-payments.jpg' | asset_img_url: 'master' }}" loading="lazy" width="250" height="36" />
{%- endif -%}